{% extends 'base.html.twig' %}

{% block title %}Hello MiscController!{% endblock %}

{% block body %}
    <h1>Contattaci</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.email) }}
    {{ form_row(form.message) }}
    <button class="btn btn-primary">Invia</button>
    {{ form_end(form) }}
{% endblock %}
